ACEAPP HISTOLOGY — COPY/PASTE WORKFLOW

You are working on the separate repository `aceapp-histology`.

GOAL
Turn the supplied Aceapp Histology v13 `index.html` into the deployable Histology
website while preserving the existing Aceapp visual identity and all validated
histology learning behavior.

NON-NEGOTIABLES
1. Do not remove or simplify the current M01–M18 curriculum, real-micrograph rotation,
   continuous-card learning, shuffled end-of-batch quiz, reverse image recognition,
   Compare mode, Spotter mode, Stats, weak-spot repair, or QA holds.
2. Histology stays visual-first. No long lectures or theory pages.
3. Never promote a review-only/preview-only image into graded practice merely to fill
   a gap. Histologic precision is more important than completion percentage.
4. Do not replace real micrographs with generic illustrations when a practical
   micrograph is required.
5. Preserve image source/license metadata.
6. Keep this repository separate from Aceapp Anatomy.

FIREBASE
Reuse the SAME Firebase project already used by Aceapp Anatomy:
`aceapp-70837`.

Use the same Firebase Authentication so one Aceapp login works in both subjects.

IMPORTANT DATA ISOLATION:
- Do NOT overwrite Anatomy progress in `users/{uid}.progress`.
- Store Histology cloud progress at:
  `users/{uid}/subjects/histology`
- Keep the existing Anatomy user document untouched.
- Use the included `firestore.rules`.
- On first Histology login, migrate/merge local progress from
  `ace-histology-progress-v1` into the Histology subject document.
- Debounce Firestore writes rather than writing after every answer.
- On logout, remove signed-in progress from the local UI but leave cloud data intact.
- Add the Histology Netlify/custom domain to Firebase Authentication authorized domains.

ACCOUNT / ACCESS
Use the Anatomy authentication flow as the behavioral reference so Aceapp feels like
one ecosystem. Do not create a second Firebase account system.

DEPLOYMENT
1. Repository name: `aceapp-histology`
2. Root deploy file: `index.html`
3. Host it as a SEPARATE Netlify site from Anatomy.
4. Connect only the `aceapp-histology` repository to that Netlify site.
5. Use the included `netlify.toml`.
6. After Netlify creates the site URL, add that exact domain to Firebase Authentication
   Authorized domains.
7. Test signup, login, logout, refresh, and cloud progress on the deployed URL.

QA BEFORE EVERY RELEASE
- Open every module M01–M18.
- Test every non-building learning batch.
- Complete at least one full shuffled batch quiz.
- Verify image→name and name→image questions.
- Verify a wrong answer shows the correct visual discriminator.
- Verify real-image variants stay locked during a question.
- Verify Compare mode does not swap images after an answer.
- Verify Spotter advances to a new locked variant.
- Verify review-only/preview-only items never enter global graded practice.
- Verify mobile layout at narrow Android width.
- Verify dark/light theme.
- Verify Stats after correct and wrong answers.
- Verify sign-in does not overwrite Anatomy progress.
- Verify Histology progress restores on another browser/device after login.
- Verify logout clears signed-in Histology state from the local UI.
- Check the console for uncaught errors.

IMAGE POLICY
For development, remote Wikimedia source URLs may be used.
For production, migrate approved originals into the repository/object storage and stop
depending on hotlinks. Keep all required attribution/license records.

DO NOT
- put Histology into the Anatomy repository;
- create a second Firebase project unless there is a future legal/organizational reason;
- publish Firebase Admin credentials;
- overwrite Firestore rules with Histology-only rules that break Anatomy;
- write Histology progress to the Anatomy `progress` field;
- convert QA-held concepts such as PCT-vs-DCT into graded questions without an
  unequivocal matched source pair.

DELIVERABLE
Return a deploy-ready repository with:
- `index.html`
- shared Firebase Authentication integrated
- Histology progress syncing at `users/{uid}/subjects/histology`
- `firestore.rules`
- `netlify.toml`
- setup/readme notes
- no regressions in the v13 learning system
- a concise release/QA report
